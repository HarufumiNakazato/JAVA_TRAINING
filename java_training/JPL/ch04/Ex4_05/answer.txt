TreeNode:抽象クラス -> 
様々な問題へ適用できそうなので、一部のメソッドを実装しておく。抽象メソッドがあったほうが拡張性が高いと思う。

TreeWalker: 具象クラス
ある特定の問題へ適用できそうなクラス。
深さ優先、幅優先などはメソッドで対応して良い。

Drawable:インタフェース
名前の通り。
グラフィックシステムにより描画可能なオブジェクトというのは対象が広いので、インタフェースにしたほうが便利。

Application: 具象クラス
ある特定のアプリケーションを指すならば、具象クラスとなる。
